{
  "name": "@allnulled/timeformat",
  "version": "1.0.1",
  "description": "Parser simple para fecha, hora, rango, duración y lista.",
  "main": "timeformat.js",
  "scripts": {
    "up": "npm run build && bash up.sh",
    "build": "npx pegjs --format globals --export-var Timeformat_parser -o timeformat.js timeformat.pegjs && node fix1.js && npm run test",
    "test": "node test.js"
  },
  "keywords": [
    "time",
    "parser",
    "format"
  ],
  "author": "allnulled",
  "license": "WTFPL"
}
